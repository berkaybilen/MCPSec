- id: rule-path-traversal
  name: Path Traversal Block
  flag: path_traversal
  mode: block
  redact: false
  enabled: true

- id: rule-sql-injection
  name: SQL Injection Block
  flag: sql_injection
  mode: block
  redact: false
  enabled: true

- id: rule-injection-detected
  name: Prompt Injection Alert
  flag: injection_detected
  mode: alert
  redact: false
  enabled: true

- id: rule-credential-leak
  name: Credential Leak Redact
  flag: credential_leak
  mode: alert
  redact: true
  enabled: true

- id: rule-sensitive-path
  name: Sensitive Path Access
  flag: sensitive_path
  mode: alert
  redact: false
  enabled: true

- id: rule-dangerous-sql
  name: Dangerous SQL Command
  flag: dangerous_sql
  mode: alert
  redact: false
  enabled: true

- id: rule-high-frequency
  name: High Frequency Calls
  flag: high_frequency
  mode: alert
  redact: false
  enabled: true

- id: rule-off-hours-access
  name: Off-Hours Access
  flag: off_hours_access
  mode: log
  redact: false
  enabled: true
