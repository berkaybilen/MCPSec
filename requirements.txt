# Python 3.11+ required
pydantic>=2.0
pyyaml
fastapi
uvicorn[standard]

# Optional: semantic similarity scoring in Toxic Flow analysis
# If not installed, semantic scoring is skipped gracefully
sentence-transformers

# Development / testing
pytest
pytest-asyncio
